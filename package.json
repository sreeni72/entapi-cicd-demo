{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for store postman collection",
  "directories": {
    "tests": "tests"
  },
  "scripts": {
    "app-tests-prod": "newman run tests/entapi-cicd.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml --insecure"
  },
  "author": "Test Author",
  "dependencies": {
    "newman": "^4.3.1"
  }
}